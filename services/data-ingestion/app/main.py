"""
Data ingestion entry point — run as a one-shot script via GitHub Actions,
not a long-running server.
"""


def main():
    print("Data ingestion job — no sources wired yet (Phase 1)")


if __name__ == "__main__":
    main()
